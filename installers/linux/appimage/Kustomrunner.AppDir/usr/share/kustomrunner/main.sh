#!/bin/bash
# Main portable launcher

DIR="$(cd "$(dirname "$0")" && pwd)"
CONFIG="$DIR/config.json"

echo "Launching Kustomrunner..."
echo "Using config at: $CONFIG"

# Future launcher logic goes here
